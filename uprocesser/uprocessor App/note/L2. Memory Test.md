test example : ddr
test 0 : OCM
test 1 : OCM + halfword + same value
tset 3 : OCM + halfword + offset to startpoint

I Cache -> enable
D Cache -> disable
보다 공정한 환경에서  DDR과 OCM을 비교하기 위해 하는 것.

1. O0 + D cache Enable
2. O0 + D cache Disable
3. O3 + D cache Enable
4. O3 + D cache Disable